
## Commit-and-push follow-up (2026-09-13, owner-authorized)

- Owner authorized commit+push across all repos. Core shipped as
  3c037ce (+ b8997b2, 10c6a4c), Marketplace as 8c41a78 (+ 2260dd4),
  Firmware as 40a4fc8.
- Pre-commit scan fallout, all fixed before push:
  1. RealtimeServerTest's proc_open harness was hard-flagged —
     modernized to Symfony Process (parameter-list launch, no shell;
     pipes → Process liveness/output; stop() for terminate). The
     socket-level test design is unchanged. Windows lesson: SIGTERM is
     undefined on Windows PHP — stop(0) is the portable terminate.
  2. The first push went RED on CI — 7 jobs MissingAppKeyException.
     Root cause: MY productization commit lost a newline in
     .env.example, fusing APP_ENV=local and APP_KEY= so CI's
     key:generate could not produce a usable key (hand-maintained
     local .env masked it). Fixed + pinned by
     env_example_is_wellformed() in DocumentationTest (b8997b2).
  3. The Firmware commit was blocked by a workspace-wide scan finding
     in Marketplace's content_audit.py (SSRF) — the fetch is now
     http-only + loopback allowlist + resolved-IP check + redirects
     refused (Marketplace 2260dd4), with a staleness note: the
     needles reflect the 2026-09-06 pre-Pulse inventory; PagesTest is
     the current-facing contract.
- Marketplace note: contact.blade.php's autocomplete="off" change (not
  authored this session) rode along with the owner's knowledge of
  their own tree.
